server.port=8090
server.error.path=/error

## A type alias is simply a shorter name for a Java type. It's only relevant to the XML configuration and simply exists to reduce redundant typing of fully qualified classnames. For example:
mybatis.type-aliases-package=com.lipop.microhouse.modle
## Enables automatic mapping from classic database column names A_COLUMN to camel case classic Java property names aColumn.
mybatis.configuration.map-underscore-to-camel-case=true
## Sets the number of seconds the driver will wait for a response from the database.
mybatis.configuration.default-statement-timeout=3000
## Sets the driver a hint as to control fetching size for return results. This parameter value can be override by a query setting.
mybatis.configuration.default-fetch-size=100
## Globally enables or disables any caches configured in any mapper under this configuration.
mybatis.configuration.cache-enabled=false
## Allows JDBC support for generated keys. A compatible driver is required. This setting forces generated keys to be used if set to true, as some drivers deny compatibility but still work (e.g. Derby).
mybatis.configuration.use-generated-keys=true
mybatis.mapper-locations=classpath:mappers/*.xml

### druid配置

spring.druid.driverClassName=com.mysql.jdbc.Driver
spring.druid.url=jdbc:mysql://139.199.12.141:3306/house?characterEncoding=UTF-8&useSSL=true
spring.druid.username=house
spring.druid.password=hBYDC3tXmYzTd2Yc
#最大连接数
spring.druid.maxActive=30
#最小连接数
spring.druid.minIdle=5
#获取连接的最大等待时间
spring.druid.maxWait=10000
#解决mysql8小时的问题
spring.druid.validationQuery=SELECT 'x'
#空闲连接检测时间间隔
spring.druid.timeBetweenEvictionRunsMillis=60000
#空闲连接最小空闲时间
spring.druid.minEvictableIdleTimeMillis=300000

spring.freemarker.charset=UTF-8
spring.freemarker.content-type=text/html;charset=UTF-8
spring.freemarker.expose-request-attributes=true
spring.freemarker.expose-session-attributes=true
spring.freemarker.expose-spring-macro-helpers=true
spring.freemarker.suffix=.ftl

spring.freemarker.settings.datetime_format=yyyy-MM-dd HH:mm:ss
spring.freemarker.settings.default_encoding=UTF-8
spring.freemarker.settings.auto_import=common/common.ftl as common



